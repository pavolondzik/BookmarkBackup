name: bookmarkbackup

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: appname
      POSTGRES_PASSWORD: bookmark
      POSTGRES_DB: bookmark_backup
    ports:
      - "5432:5432"
    volumes:
      - bookmark_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U appname -d bookmark_backup"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.fastapi
    environment:
      DATABASE_URL: postgresql+psycopg://appname:bookmark@db:5432/bookmark_backup
    ports:
      - "8000:8000"
    volumes:
      - ../src:/app/src
      - ../frontend:/app/frontend
    depends_on:
      db:
        condition: service_healthy
    command:
      - sh
      - -c
      - >-
        alembic upgrade head &&
        exec uvicorn bookmark_backup.web.app:app
        --host 0.0.0.0 --port 8000 --reload

  web:
    build:
      context: ../frontend
      dockerfile: ../docker/Dockerfile.web
    environment:
      VITE_API_PROXY_TARGET: http://api:8000
    ports:
      - "5173:5173"
    volumes:
      - ../frontend:/app
      - /app/node_modules
    depends_on:
      - api

volumes:
  bookmark_pg_data:
