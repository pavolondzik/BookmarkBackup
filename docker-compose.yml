services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: pavol
      POSTGRES_PASSWORD: bookmark
      POSTGRES_DB: bookmark_backup
    ports:
      - "5430:5432"
    volumes:
      - bookmark_pg_data:/var/lib/postgresql/data

volumes:
  bookmark_pg_data:
